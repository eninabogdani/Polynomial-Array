{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/poly.cpp",
                "${workspaceFolder}/lab1.cpp",
                "-o",
                "${workspaceFolder}/a.out",
            ]
        }
    ]
}